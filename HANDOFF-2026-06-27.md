# Handoff — JB Digital System · 2026-06-27

## Contexto da sessão
Auditoria completa do sistema + início da migração de pricing + planejamento da automação de criação de sites.

---

## ✅ O que foi feito

### Stripe
- 2 produtos criados no LIVE: `prod_Umd1UoGUBn8L6L` (Essencial) · `prod_Umd43OUyxt1W8J` (Premium)
- 4 price IDs novos:
  - `price_1Tn3pZAtjEQC8UwgpNeWkETN` — Essencial Setup R$600 (one-time)
  - `price_1Tn3rJAtjEQC8Uwgd1xFvNDW` — Essencial Mensal R$150/mês
  - `price_1Tn3twAtjEQC8UwgH7plWPMp` — Premium Setup R$1.200 (one-time)
  - `price_1Tn3wxAtjEQC8UwgC05O2k33` — Premium Mensal R$350/mês

### Código (commit `b6a516f`, aguardando push)
- `supabase/functions/create-billing-checkout/index.ts` — migrado para tiers Essencial/Premium, `mode: 'subscription'`, setup+mensal como line_items
- `src/pages/ClientBilling.tsx` — 3 planos antigos → 2 tiers novos com setupPrice + monthlyPrice

---

## ⚠️ AÇÕES IMEDIATAS (fazer antes da próxima sessão)

### 1. Rotacionar chave Stripe (URGENTE)
A chave `sk_live_51OGP...` ficou exposta no chat. Crie uma nova em:
Stripe Dashboard → Desenvolvedores → Chaves de API → Criar chave secreta

Atualize o secret no Supabase:
```bash
npx supabase secrets set STRIPE_SECRET_KEY=sk_live_NOVA_CHAVE --project-ref rqiacrclsmixmaqhgicc
```

### 2. Push GitHub
```bash
cd /Users/julianabarcellossilva/jb-digital-solutions
git push origin main
```

### 3. Arquivar produtos antigos no Stripe
```bash
SK="sua_nova_sk_live" && \
for PROD in prod_Tz5LDgrTB1PbG5 prod_Tz5LsTHFuyiCql prod_Tz5LnqlcTmpG6D prod_Tz5RRdjUd6I2Ky; do \
  curl -sS -X POST "https://api.stripe.com/v1/products/$PROD" -u "$SK:" -d "active=false" | python3 -c 'import sys,json; r=json.load(sys.stdin); print(r["id"], "archived" if not r.get("active") else "ERR")'; \
done
```

### 4. Deploy da edge function atualizada
```bash
cd /Users/julianabarcellossilva/jb-digital-solutions
npx supabase functions deploy create-billing-checkout --project-ref rqiacrclsmixmaqhgicc
```

---

## 📋 Pendências — próxima sessão

### Fase 2 — Pipeline de automação (PRIORIDADE MÁXIMA)
Criar em `site-engine/scripts/`:

| Arquivo | O que faz |
|---------|-----------|
| `onboard-client.ts` | Orquestrador: lê intake Supabase → gera `[slug].ts` → registra em `index.ts` → converte imagens → deploy |
| `lib/intake-to-clientdata.ts` | Mapper: slug, specialtyNoun, geocoding, openingHours, services[], neighborhoods[] |
| `gen-og-image.py` | Pillow: og:image 1200×630 com cores da marca |
| `convert-webp.py` | Conversão de fotos para WebP 4:5 |

Pré-requisito: adicionar `ANTHROPIC_API_KEY=sk-ant-...` em `site-engine/.env.local`

Controle de tier no engine:
- **Essencial** = `neighborhoods: []` → one-page (padrão Ariane)
- **Premium** = `neighborhoods: [...]` → +páginas de bairro (padrão Marcela)

### Fase 3 — Questionário
- Adicionar campo `gbpUrl` no `src/pages/Onboarding.tsx` Step 0 (Google)
- Awareness de tier para exigir `regions` só no Premium

### Fase 4 — Verificação dashboard
- Walkthrough visual logado como client: NewProject → wizard → timeline → review → billing

---

## Decisões arquiteturais desta sessão

| Decisão | Escolha | Motivo |
|---------|---------|--------|
| Modelo de preço | Setup + mensalidade | Recorrência + cobertura setup |
| Geração de site | Híbrido (automação + revisão) | Qualidade previsível, escalável |
| Essencial vs Premium | One-page vs páginas de bairro | Mesmo engine, neighborhoods[] vazio ou não |
| Stripe checkout | mode:subscription com setup one-time | Cobrar setup na 1ª fatura, mensal nas seguintes |

---

## Como iniciar a próxima sessão

```
"Retomar handoff HANDOFF-2026-06-27.md do jb-digital-solutions.
Começar pelas ações imediatas (push, archive Stripe, deploy edge function),
depois Fase 2 automação (onboard-client.ts)."
```
