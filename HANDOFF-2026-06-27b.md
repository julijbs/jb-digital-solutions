# Handoff — JB Digital System · 2026-06-27 (Sessão 2)

## O que foi feito nesta sessão

### Ações imediatas (feitas agora)
- ✅ `git push origin main` — commits `b6a516f` (billing), `7b3e94a` (handoff), `8fc7e2d` (features) publicados
- ✅ Edge function `create-billing-checkout` deployada no Supabase
- ✅ 4 produtos Stripe legados arquivados via MCP OAuth (sem expor chave)

### Features implementadas (commit `8fc7e2d`)
- ✅ **NewProject.tsx** — seletor Essencial/Premium antes do wizard; salva `projects.plan`
- ✅ **Onboarding.tsx Step 0** — campo URL do GBP aparece quando usuário responde "Sim"; salvo em `google_data.gbp_url` + `projects.gbp_url`
- ✅ **Onboarding.tsx Step 2** — campo de bairros SEO só aparece para clientes Premium
- ✅ **onboard-client.ts** — orquestrador: lê intake Supabase → gera copy via Claude Sonnet → escreve `src/data/<slug>.ts` + registra em `index.ts`
- ✅ **intake-to-clientdata.ts** — mapper completo intake→ClientData (brand tokens, openingHours, neighborhoods, copy AI)
- ✅ **convert-webp.py** — converte fotos para WebP 4:5 (`pip install Pillow`)
- ✅ **gen-og-image.py** — gera og:image 1200×630 com cores da marca

---

## ⚠️ AÇÕES URGENTES (antes da próxima sessão)

### 1. Rotacionar chave Stripe (CRÍTICO)
A chave `sk_live_51OGP...` foi exposta no chat desta sessão.
1. Stripe Dashboard → Desenvolvedores → Chaves de API → Criar nova chave secreta
2. Atualizar secret no Supabase:
```bash
npx supabase secrets set STRIPE_SECRET_KEY=sk_live_NOVA_CHAVE --project-ref rqiacrclsmixmaqhgicc
```

### 2. Configurar site-engine/.env.local
Crie o arquivo `site-engine/.env.local` com:
```
SUPABASE_URL=https://rqiacrclsmixmaqhgicc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<pegar no Supabase Dashboard → Settings → API → service_role>
ANTHROPIC_API_KEY=sk-ant-...
CF_API_TOKEN=<seu token Cloudflare>
CF_ACCOUNT_ID=<account id Cloudflare>
CF_ZONE_ID=<zone id do jbdigitalsystem.com>
```

### 3. Instalar Pillow (Python)
```bash
pip install Pillow
```

### 4. Revogar token Cloudflare exposto
Cloudflare Dashboard → My Profile → API Tokens → Revogar token antigo

---

## 📋 Próxima sessão — por onde começar

### Passo 1: Testar fluxo cliente E2E (15 min)
```
npm run dev
```
Abrir localhost:5173, fazer login como cliente e testar:
- Novo Projeto → seletor de plano aparece? ✓
- Onboarding Step 0 → campo GBP URL aparece ao clicar "Sim"? ✓
- Onboarding Step 2 → bairros aparecem só para Premium? ✓
- Dashboard → timeline de status funciona?
- Billing → planos Essencial/Premium aparecem?

### Passo 2: Testar automação (10 min)
Pegar um `project_id` de um projeto com intake concluído no Supabase e rodar:
```bash
cd site-engine
npm run onboard <project_id>
```
Verificar o arquivo `src/data/<slug>.ts` gerado. Ajustar `lat`/`lng` se necessário.

### Passo 3: Fase 3 — Se quiser continuar
Verificar se há outros gaps no questionário de onboarding.

---

## Como retomar
```
"Retomar handoff HANDOFF-2026-06-27b.md. Começar com teste E2E do fluxo cliente no localhost:5173, depois primeiro npm run onboard com um project_id real."
```
