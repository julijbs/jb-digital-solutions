# Handoff — JB Digital System · 2026-06-27 (Sessão 3)

## O que foi feito nesta sessão

- ✅ `vercel env pull` — populou `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` no `.env.local` raiz
- ✅ Fix bug billing: `create-billing-checkout` redirect `/billing` → `/dashboard/billing` (commit `9066a10`, deployado)
- ✅ E2E completo aprovado visualmente (GIF salvo em Downloads: `e2e-jb-digital-cliente-premium.gif`)
- ✅ Decisão arquitetural: copy gerado por **Claude Code** (Max), não via Anthropic API direta

## E2E — todos os checkpoints ✅

| Feature | Status |
|--------|--------|
| Signup sem confirmação de e-mail | ✅ |
| NewProject — seletor Essencial/Premium | ✅ |
| Onboarding Step 0 — GBP URL condicional | ✅ |
| Onboarding Step 3 — bairros só Premium | ✅ |
| Dashboard — card + timeline 6 milestones | ✅ |
| Billing — planos + preços corretos | ✅ |

## ⚠️ Ações urgentes (Juliana faz antes de abrir próxima sessão)

1. **`git push origin main`** — commit `9066a10` (billing fix) ainda local
2. **Rotacionar chave Stripe** `sk_live_51OGP...` exposta em sessão anterior
3. **Revogar token Cloudflare** exposto em sessão anterior

## Próxima sessão — passo a passo

### 1. Refatorar `intake-to-clientdata.ts` (remover Anthropic API)

**Arquivo:** `site-engine/scripts/lib/intake-to-clientdata.ts`

O que muda:
- Remover parâmetro `anthropic: Anthropic` da função `intakeToClientData`
- Remover o bloco `anthropic.messages.create(...)` (linhas ~122-131)
- Usar diretamente o fallback de copy estruturado que já existe (linhas ~135-170)
- Adicionar campo `_copyPrompt` no retorno com o prompt completo pronto para colar no Claude Code

**Arquivo:** `site-engine/scripts/onboard-client.ts`

O que muda:
- Remover `import Anthropic` e `new Anthropic()`
- Remover validação de `ANTHROPIC_API_KEY` (linhas 50-56)
- Remover `ANTHROPIC_API_KEY` da lista de env vars necessárias
- Após escrever o `.ts`, imprimir o `_copyPrompt` no console para uso no Claude Code

### 2. Fluxo novo pós-refatoração

```bash
cd site-engine
npm run onboard <project_id>
# → escreve src/data/<slug>.ts com dados estruturados + copy placeholder
# → imprime prompt no console

# No Claude Code: cole o prompt → gero copy → atualizo o .ts
```

### 3. Primeiro onboard real

**project_id de teste já criado:** `6814f862-028e-49b2-9f1b-a2f7d989ba94`
("Clínica Teste JB", Premium, São Paulo/SP, Psicólogo(a))

Antes de rodar: adicionar ao `.env.local` raiz (o script lê dali, não de `site-engine/.env.local`):
```
SUPABASE_SERVICE_ROLE_KEY=<Supabase → Project Settings → API → service_role>
```

Rodar:
```bash
cd site-engine && npm run onboard 6814f862-028e-49b2-9f1b-a2f7d989ba94
```

Inspecionar `src/data/clinica-teste-jb.ts` → Claude Code gera copy → limpar dados de teste após validar.

### 4. Pipeline completo acordado (NÃO ALTERAR)

```
logo do cliente
  → extract-brand.ts (Gemini 2.5 Flash via Lovable Gateway) → brand tokens
  → onboard-client.ts (lê intake Supabase) → src/data/<slug>.ts (estrutura)
  → Claude Code (sessão Max) → copy personalizado → atualiza .ts
  → Astro build → Cloudflare Pages deploy
```

**Sites são personalizados, não templates.** Mesmo formato/estrutura Astro, copy e design únicos por cliente derivados do logo e intake.

## Como retomar

```
"Retomar HANDOFF-2026-06-27c.md. Começar com git push, depois refatorar intake-to-clientdata.ts para remover Anthropic API, depois primeiro npm run onboard real."
```
